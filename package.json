{
  "name": "olai",
  "version": "0.1.0",
  "private": true,
  "description": "Olai, the analyst agent that buys its own intelligence and trades under a written rulebook",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "better-sqlite3@13.0.3": true
  }
}
