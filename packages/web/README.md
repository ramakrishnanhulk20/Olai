# Olai web

The owner-facing app for Olai: the landing page, the lab page where visual direction is
approved, and later the desk where the rulebook, the proposals and the ledger live.

Run it with `npm run dev -w @olai/web` from the repo root, then open http://localhost:3000/lab.
